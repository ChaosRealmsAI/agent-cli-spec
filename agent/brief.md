agent-cli-lint — Agent-Friendly CLI Spec v0.1 合规检查工具

检查任意 CLI 是否符合 Agent-Friendly CLI Spec 的 11 维度 98 条规则。
输出结构化报告，按 `core / recommended / ecosystem` 三层展示覆盖率与认证等级（Agent-Friendly / Agent-Ready / Agent-Native）。
支持动态执行检查、静态文件检查、快照对比、AI 辅助 prompt 生成。
