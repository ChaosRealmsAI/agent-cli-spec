# Archive

Archived material kept for reference but not part of the active product surface.

- `examples/`: old sample CLIs and prototype implementations
- `spec/`: spec drafts, rendered docs, and human-readable exports
- `test-fixtures/`: historical fixture projects and experiment sandboxes

The active repository surface is now centered on:

- `agent-cli-lint`
- `rust/agent-cli-lint`
- `skills/`
- `agent/`
- `data/`
