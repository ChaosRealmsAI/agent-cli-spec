# note

Local note-taking CLI. Default output is JSON.

## Usage

```bash
note add --title "..." --body "..."
note list
note show <id>
note search --query "..."
note delete <id> --yes
```

## Dependencies

bash + jq
